"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
}
{
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "tailwindcss": "^4.2.1",
    "tailwindcss-animated": "^2.0.0"
  }
}
